<nav class="bg-black p-4 text-gray-200 text-sm md:text-base lg:text-lg">
    <div class="flex justify-between items-center">
        <a href="{{ path('default_home') }}" class="font-bold text-white">Mini-Réseau</a>

        <div id="navbarNav">
            <ul class="flex gap-4 items-center">
                {% if app.user %}
                    <li>
                        <a href="{{ path('default_home') }}" class="hover:text-purple-400">Accueil</a>
                    </li>
                    <li>
                        <a href="{{ path('message_add') }}" class="hover:text-purple-400">Rédiger un post</a>
                    </li>

                    {% if 'ROLE_ADMIN' in app.user.roles %}
                        <li>
                            <a href="{{ path('admin_users') }}" class="hover:text-purple-400">Gérer les utilisateurs</a>
                        </li>
                    {% endif %}
                {% endif %}

                <li>
                    <button id="toggleDarkMode" type="button" class="px-3 py-1 rounded bg-gray-100 dark:bg-gray-700 text-sm" aria-label="Basculer thème" title="Basculer thème">
                        🌗
                    </button>
                </li>
            </ul>
        </div>

        <ul class="flex gap-4 items-center">
            {% if app.user %}
                <li>
                    <a href="{{ path('app_logout') }}" class="hover:text-purple-400">Déconnexion</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
